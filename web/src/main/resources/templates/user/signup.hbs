{{#partial "content"}}
    <main class="wrap section--tight">

        <nav class="label" aria-label="Breadcrumb" style="margin-bottom:var(--s-4)">
            <a href="index.html">User</a> / <span>Signup</span>
        </nav>

        {{#if formErrors}}
            <h3>Signup failed</h3>
        {{/if}}

        {{#each formErrors}}
            <h3>{{this}}</h3>
        {{/each}}

        <form action="/user/signup" method="POST" class="grid">
            <div class="field">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" class="input" required placeholder="you@example.com">
            </div>
            <div class="field">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" class="input" required>
            </div>
            <div class="field">
                <label for="repeat_password">Repeat password:</label>
                <input type="password" id="repeat_password" name="repeat_password" class="input" required>
            </div>
            <div class="field">
                <button type="submit" class="btn btn--primary">Signup</button>
            </div>
        </form>

    </main>

{{/partial}}

{{>layout}}